# Script — {slug}

> Narration with visual anchors, section by section. Check against
> `drafts/_writing-rules.md` before recording. A clean prose-only copy goes
> in `script-recording.md` for reading aloud.
> Budget ~150 words per minute of target runtime.

## HOOK (frames 0–N)

> <narration…>

**Visual anchors**
- "<spoken phrase>" → <on-screen event> (~fN)

## <SECTION> (frames N–M)

> <narration…>

**Visual anchors**
- "<spoken phrase>" → <on-screen event> (~fN)

## OUTRO (frames M–TOTAL)

> <narration — drop the energy, dry CTA, end on the URL>

**Visual anchors**
- "<spoken phrase>" → <CTA lockup>

---

## After recording

1. Save the face-crop mp4 into `public/` and export a transcript/SRT.
2. Build `sync-map.md` from the transcript (audio is now the source of truth).
3. Re-time the composition's ANCHORS from the sync map.
   See `docs/narrated-workflow.md`.
